export enum Axis {
  All,
  Vertical,
  Horizontal,
}

export type Coordinates = {
  x: number;
  y: number;
};

export type RectType = {
  top: number;
  left: number;
  right: number;
  bottom: number;
};
